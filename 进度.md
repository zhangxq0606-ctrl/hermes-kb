# 当前目标

auto_sync.ps1 自动同步脚本定时任务，每 10 分钟自动同步全链路。

# 当前阶段

auto_sync 定时任务（已完成）

# 当前阻塞

无

### 本阶段待办（按优先级）

- [x] auto_sync.ps1 修正（类型转换 + python 全路径）
- [x] 注册 HermesAutoSync 定时任务（每 10 分钟）
- [x] 全链路验证无报错

# 已完成

- [x] auto_sync.ps1 修正 + HermesAutoSync 定时任务注册（每 10 分钟自动同步全链路）
- [x] 前端"编译论点"tab 接入：build_static.py CATEGORIES 新增 topic（排首位）+ discover_docs topic 独立遍历 + generate_browse 📖前缀+琥珀色#d97706；web_server.py BROWSE_ROOTS/TAB_LABELS/SECTION_LABEL 扩展 + browse() topic 独立卡片渲染 + tab CSS
- [x] compiler.py 创建：四阶段设计（精炼扫描 → 聚类/标签继承 → AI 编译 → topic 文件写入），独立可运行，JM 输出
- [x] main.py 管线追加 COMPILER 步骤（WEEKLY_SCAN 之后，parse_json=True, critical=False）
- [x] usage_tag 标签体系：SYSTEM_PROMPT 新增 usage_tag JSON schema 字段 + 标签选择规则，route_file 写入 `> 标签: {usage_tag}` 至 _refined.md 及 split 文件，bootstrap.py REQUIRED_DIRS 追加 core/topic/
- [x] 架构总览 v2.0 设计
- [x] main.py 统一管线调度器
- [x] bootstrap.py 启动自检 + Syncthing 守护
- [x] hermes_engine.py 核心引擎（三阶段事务模型、死信队列、细胞分裂、URL 冷备、前置知识唤醒）
- [x] index_guard.py SHA256 + mtime 增量索引
- [x] weekly_scan.py 摘要级周扫 + Dashboard Active/Backlog 双区管理
- [x] 全管线跑通（2026-05-28 日志确认 system_ready=true）
- [x] Dashboard 问题队列机制落地（3 Active / 9 Backlog）
- [x] 架构.md 创建

- [x] Syncthing Windows 服务注册（HermesSyncthing，NSSM，开机自启）
- [x] 修复多实例锁冲突（清理残留进程，统一由服务管理）
- [x] Cloudflare Tunnel 穿透 + 手机端验证
- [x] 首页精简：删除索引加载行
- [x] 浏览页标签式布局（硬知识 / 软智慧 / 问题拷问）+ 精炼/原始筛选
- [x] Dashboard 问答回路：POST /answer/save 接口（写入回答 + 生成 inbox 笔记）
- [x] 问题查看页回答 UI（写回答 / 编辑回答 / 保存 → 刷新）
- [x] 浏览页已答问题显示 "✓ 已回答" 绿色标签
- [x] 知识分类体系重构：technical/insight → hard/soft，引擎 SYSTEM_PROMPT + route_file 全链路更新
- [x] 浏览页标签文案同步：硬知识 / 软智慧 / 问题拷问
- [x] AI 提纯分支策略：soft 用认知骨架，hard 用技术骨架，mixed 细胞分裂各自使用对应模板
- [x] 静态浏览层：build_static.py 生成脚本（遍历 _refined.md → 静态 HTML，Markdown 渲染 + Pagefind 搜索）
- [x] GitHub Action 部署配置：.github/workflows/deploy-pages.yml（push 自动部署至 Cloudflare Pages）
- [x] 维护：消除 web_server.py `\*` SyntaxWarning（已验证无此警告，文件已含 `\\*`）
- [x] 维护：架构.md 重命名为 架构.md，更新进度.md 引用
- [x] 维护：创建 docs/ 目录，补齐工作流规范目录结构
- [x] 维护：安装 python-dotenv，消除 Flask 启动提示
- [x] 分类逻辑优化：收紧 mixed 判定标准（hard/soft 追加 guard 规则，评分追加 ≥5 阈值），route_file 按目标目录选骨架写入 _refined.md，清理 Superpowers介绍 错分至 core/insight 的 3 个文件
- [x] 浏览页初始过滤修复：首次加载直接隐藏 data-type="original" 卡片（替代不可靠的 .filter.active click 触发）
- [x] 周扫审计重构：AUDIT_PROMPT 禁止跨域硬关联 + 允许空输出（0~3条），parse_questions 移除 fallback，新增空 questions 保护，清理 3 条牵强问题，Dashboard/state 已重置
- [x] 品牌重命名：Hermes → Xq.KB，覆盖 web_server.py / build_static.py / Dashboard / main.py / bootstrap.py / scheduler.py 共 14 处对外文案，grep 验证零残留
- [x] 首页重设计：三区色彩体系（蓝#2563eb/琥珀#d97706/紫#7c3aed），分类入口卡片 + 左侧 4px 色条，全局 #fafbfc 背景，JS 逻辑全保留
- [x] 全站样式统一：浏览页 tab 三区激活色 + 卡片去渐变，详情页代码块深色主题（#1e293b/#e2e8f0）+ 引用块紫色条，静态站 STYLE_CSS 三区色卡片 + 重新生成 7 篇文档，旧色 #4a90d9/#f5f5f5 零残留
- [x] 静态站 <title> 修复：首页 make_head("首页") → "首页 - Xq.KB"，消除双品牌名
- [x] Pagefind 搜索索引重建：11 页 / 708 词，已生成 public/pagefind/
- [x] Skill 集合更新：架构师.md 新增「逐步下发原则」，工作流总览.md 闭环节奏新增「一次只输出一条」约束
- [x] 双轨 wikilinks：collect_background_context 扩展全量笔记（3目录/去上限/分组格式），SYSTEM_PROMPT 追加 [[wikilinks]] 规则（hard/soft/mixed 三策略），渲染层 [[xxx]] → 紫色虚线下划线链接（web_server /view?q= + build_static /browse/insight.html?q=）
- [x] wikilinks 跳转解析闭环：web_server 新增 /wikilink/<name> 302 重定向路由 + build_file_index() 辅助函数（12 条目，5 分钟 TTL 缓存，_refined.md 优先），_render_markdown wikilinks 正则改为 /wikilink/ 协议；build_static 编译期 slug→URL 查表替换（generate_detail 预先构建 slug_to_url 映射），命中则链接至 /detail/<key>/<slug>.html，未命中优雅降级至 /browse/insight.html?q=
- [x] AI 分类时效性标记：SYSTEM_PROMPT 新增 timeliness 字段（high/medium/low）+ 三段判断标准（核心方法论/一般知识/高度时效），route_file 在 refined 文件末尾追加 `> ⏱️ 时效性: {timeliness}`，不影响现有分类/路由逻辑
- [x] wikilinks 聚合页：hermes_engine 新增 aggregate_wikilinks() 函数（扫描 refined/split 中 [[wikilinks]]，按 hard→entity / soft→concept 路由，创建/更新聚合页 + 引用片段去重），route_file 末尾调用；bootstrap.py REQUIRED_DIRS 追加 core/entity + core/concept
- [x] 聚合页渲染接入：web_server BROWSE_ROOTS + LABEL_TO_TAB 覆盖 entity/concept（归类至"软智慧"tab），build_static CATEGORIES 扩展 + discover_docs 去 _refined 后缀适配 + 首页实体/概念分类卡片（emerald 绿 #059669 / slate 灰 #64748b），浏览/详情全链路生成
- [x] 聚合页路由修复：aggregate_wikilinks refined 分支优先查已有聚合页目录（entity_path / concept_path 存在性）再 kt 兜底，避免 hard 笔记中引用概念错分至 entity；架构.md 同步更新目录结构 + 设计原则
- [x] 浏览页派生文件过滤：web_server 新增 _is_derived_file() 辅助函数，/browse 收集 entries 时排除 tech_*/insight_*/_source_* 系统派生文件；technical 从 15 篇降至 10 篇，"原始"筛选不再出现分裂产物，"全部"也不混入派生文件
- [x] 浏览页 canonical 卡片归并：新增 _canonical_key() / _entry_role() 归一化文件名，/browse 按 canonical_key 分组，同主题多文件（精炼/原文/技术拆分/认知拆分/冷备）合并为单卡片 + 版本信息行（如"精炼 + 原文 + 技术拆分 · 3天前"）；technical 15 个文件归并为 5 张卡片，insight 4 个归并为 2 张；data-type 支持多值（"refined original"），filter JS 改为 indexOf 匹配；_is_derived_file 保留不删
- [x] 修复 canonical 归并后 badge 误判：L826 `g["data_type"] == "refined"` 在多值 `"refined original"` 时恒为 False → 改为 `"refined" in g["roles"]`；修复前含 refined 的卡片误显示灰色"原始"徽章，修复后正确显示蓝色"精炼"徽章 + 蓝色左边条
- [x] /browse 页面清理：BROWSE_ROOTS 删除 core/entity + core/concept；LABEL_TO_TAB/SECTION_LABEL 删除对应行；删除聚合页强制可见代码块；删除精炼/原始/全部筛选按钮；删除 filter JS 和 init hide JS；保留 tab 切换 JS、卡片视觉层级（refined 蓝色左边条 + 预览）
- [x] 删除聚合页子系统：hermes_engine.py 删除 aggregate_wikilinks()、ENTITY/CONCEPT 常量、aggregated_parts 构建调用；split 文件写入去掉 wikilinks 头部行；bootstrap.py REQUIRED_DIRS 删除 entity/concept；build_static.py CATEGORIES/CSS/icons/descs 删除 entity/concept；删除 core/entity/ 和 core/concept/ 目录
- [x] 架构.md 更新：目录结构删除 entity/concept，设计原则删除聚合页维护行，版本号 v2.1 → v2.2
- [x] 引擎聚合页链路清理：hermes_engine 删除 ENTITY/CONCEPT 常量 + os.makedirs + aggregate_wikilinks() 函数体（~75行）+ route_file 调用点；split 文件写入简化（tech_content=tech_split, insight_content=insight_split，去 wikilinks 头部行）；引擎其余流程不受影响
- [x] entity/concept 全链路残留清理：build_static.py 删 CATEGORIES 条目 + CSS 规则 + discover_docs 中 is_aggregate 分支 + icons/descs；bootstrap.py 删 REQUIRED_DIRS；文件系统删 kb/core/entity/ + kb/core/concept/；grep 验证 kb/api/ + kb/scripts/ 零残留
- [x] 首页引用来源优化：qa_engine.py 新增 _extract_title() + _section_label()，ask/ask_stream 返回 sources 对象数组（path/title/section）；web_server INDEX_HTML 渲染改为"标题 [分类]"格式；旧缓存 5 分钟自然过期
- [x] 浏览页配对卡片布局：拆掉 canonical 合并单卡片设计，改为按主题分组双行卡片（card-group 精炼在上原文在下 / 精炼蓝色大卡片+预览 / 原文灰色小字无左边条 / 各自独立链接），_extract_meta 标题回退去 _refined 后缀；清理 filter-row CSS 残留 + origin-section 回退
- [x] 首页简化：删除三张分类入口大卡片（硬知识/软智慧/问题拷问，冗余于 /browse tab），替换为单张 📚 浏览知识库 精美卡片（蓝色左边条 + 箭头）；删除 footer 重复浏览链接及 cat-card/footer CSS；INDEX_HTML 减负 ~20 行
- [x] 全链路功能验证：8/8 通过（首页/浏览页/详情页/问答引擎/管道状态/语义索引/Wikilinks/来源格式）
- [x] 检索管线优化：semantic_index.search() 新增 min_score=0.3 阈值过滤 + 返回 (path, score, mtime) 三元组；qa_engine top_k 5→3；SYSTEM_PROMPT 重写为结构化规则（含来源引用/部分回答/无相关内容/简洁直接/中英双语）；ask/ask_stream sources 新增 mtime（YYYY-MM-DD）字段；验证通过（Rust 生命周期→"暂无相关内容"，财商的修炼→3条结果含 path/title/section/mtime）
- [x] 会话管理：qa_engine.py 新增 SessionManager 类（_sessions 内存存储，create_session/get_history/add_message/clear_session，消息上限 20 条，超出移除最早两条）；新增 chat() 函数（多轮对话，含历史上下文 + 检索结果，不使用 _ANSWER_CACHE）；web_server.py 新增 POST /chat 和 POST /chat/clear 路由；验证通过（首问返回 session_id，追问关联上下文，clear 返回 ok）
- [x] 对话链路打通：/chat 路由合并 GET+POST（GET 返回占位页）；INDEX_HTML 搜索框前插入 💬 对话入口卡片（复用 .browse-entry 样式）；验证通过（首页有对话卡片，/chat GET 返回 Chat placeholder，POST 不受影响）
- [x] 聊天界面 CHAT_HTML：完整聊天 UI（顶部栏/消息区/输入栏/返回链接），用户气泡蓝底白字右对齐，AI 气泡白底 box-shadow 左对齐 + 来源链接（/view/{path}），JS 交互（Enter 发送/Shift+Enter 换行/新对话清空），fetch POST /chat 通信；验证通过（12/12 元素检测全部 OK）
- [x] 演进发现：chat() 文档注入格式改为 "【文档 N - {date}】{path}\n{content}"（date 从 mtime 取）；构建 chat_system = SYSTEM_PROMPT + 演进检测规则（对比同主题不同时期观点 → 递进/修正/反转 → 末尾列轨迹 + 变化较大时提示整合）；SYSTEM_PROMPT 常量不变，ask/ask_stream 不受影响；验证通过（多文档不同子主题 → LLM 正确不触发轨迹，ask 无规则泄露）
- [x] URL 抓取并行化：hermes_engine.py main() 中 URL 抓取段从串行改为 ThreadPoolExecutor(max_workers=3) 并行，抓取失败时输出 URL_FETCH_FAIL 日志，不阻塞其他 URL，总等待时间 ≈ 最慢单个 URL 耗时
- [x] 处理进度反馈：hermes_engine.py main() 添加 [进度] 日志（发现 N 个文件待处理 / 序号/总数 处理中 / 序号/总数 完成+耗时 / 全部完成汇总），仅添加 log() 调用，不改处理逻辑
- [x] collect_background_context 缓存：添加模块级 _context_cache/_context_cache_time，5 分钟内多次调用直接返回缓存，不再遍历磁盘，空结果也缓存
- [x] 浏览页精炼卡片随机颜色：web_server.py 添加 CARD_COLORS 列表（10色），每个 card-group 随机分配颜色，内联 style 设置 border-left-color 和 badge 的 color/background（15% 不透明度），CSS 移除固定颜色
- [x] collect_background_context 语义检索：从全量扫描改为 semantic_index.search(content, top_k=5, min_score=0.3)，背景长度稳定不随知识库增长，缓存 key 改为 content hash，调用处传入 content 参数
- [x] semantic_index 内存缓存：_load_cache 添加 _memory_cache 常驻内存，force_reload 参数控制刷新，build_index 后自动 force_reload，连续 search 不触发磁盘 IO
- [x] usage_tag 标签体系：SYSTEM_PROMPT 新增 usage_tag JSON schema 字段 + 标签选择规则，route_file 写入 `> 标签: {usage_tag}` 至 _refined.md 及 split 文件，bootstrap.py REQUIRED_DIRS 追加 core/topic/

# 本轮完成

- [x] auto_sync.ps1 修正（互斥锁 [int] 类型强制转换 + python 全路径 C:\Users\qqmin06\python-sdk\python3.13.2\python.exe + UTF-8 BOM 编码）
- [x] HermesAutoSync 定时任务注册成功（每 10 分钟，-Once -RepetitionInterval 10min -RepetitionDuration 30天）
- [x] 手动验证：脚本语法解析通过，State=Ready，手动触发正常，全链路启动无报错

# 上一轮遗留与注意

- 部署前需在 GitHub Secrets 配置 CLOUDFLARE_API_TOKEN 和 CLOUDFLARE_ACCOUNT_ID
- 本地测试用 `python kb/scripts/build_static.py` 生成，用 `npx pagefind --site public` 建立搜索索引（Pagefind 依赖 npm registry，若代理不通可稍后重试）
- Cloudflare Pages 项目名 hermes-kb，绑定自定义域名 read.captainxq.me

# 技术债

- 密码保护暂缓：before_request 认证中间件 + 长期 Cookie + 魔法书签免输入（当前仅本地使用，暂不需要）

# 下一阶段

按需扩展：Wiki 层、面板统计、知识图谱可视化
