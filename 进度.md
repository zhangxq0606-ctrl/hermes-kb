# 当前目标

知识库全自动化运转——任何输入（SY 碎片 / CC 精炼 / CC 回答 / CC 选题）都能自动走完管线并上线静态站，无需手动干预。

# 当前阶段

自动化闭环 + 双端 CC 指令重构完成

# 当前阻塞

无

# 实施进度

## 基础设施（已完成）

- [x] API 提供商切换：引擎 + CC 统一阿里百炼 DashScope（qwen3.6-flash）
- [x] 服务器 SSH key 生成 + GitHub 公钥注册
- [x] 本地 SSH key 生成 + GitHub 公钥注册
- [x] 服务器 git remote SSH
- [x] 本地 git remote 双栈（fetch ghproxy / push SSH）
- [x] filter-repo 清理 `.env` 全部历史
- [x] GitHub Secret Scanning 报警已消除
- [x] Syncthing Windows 服务 + NSSM 自启
- [x] `.gitignore` 清理（忽略 .env、.sync.lock、.stversions/）

## 自动化闭环（当前阶段核心）

- [x] **watch_inbox.ps1**：FileSystemWatcher 监听 inbox，60s 防抖，触发引擎+建站+push
- [x] **启动 catch-up**：开机时先 git pull 同步服务器 CC 产出
- [x] **周扫自动化**：满 7 天自动触发完整管线（引擎+周扫+编译器）
- [x] **git push 自动**：add→commit→push 一步到位
- [x] **互斥锁**：`.sync.lock` 防止多实例冲突
- [x] **日志**：`kb/logs/watch_inbox.log`
- [x] **开机自启**：Windows 任务计划程序（登录时启动）
- [x] **旧任务清理**：删除 `HermesAutoSync`（10min）、`HermesKBAutoPull`（10min）
- [x] **旧脚本清理**：删除 `auto_sync.ps1`（scp 方案）
- [x] **服务禁用**：`HermesScheduler` 服务已停用

## 三端同步（已完成）

- [x] **GitHub 唯一中间人**：两端不 scp、不 ssh 互连、不 mtime 比较
- [x] **服务器 sync.sh**（cron `*/1`）：stash→rebase→stash pop→push→build
- [x] **本地 watch_inbox**：监听 inbox + 自动 git pull/push
- [x] **两端指令隔离**：本地 CLAUDE.md + skills 经 `.gitignore` 隔离，与服务器不冲突
- [x] **writing/ 协同**：服务器 CC 探讨的选题 → git → 本地 CC 接着写 → git → 上线

## 双端 CC 指令重构（已完成）

### 服务器（手机端第二大脑）

- [x] **CLAUDE.md** 重写：精简顶层规则，定位为第二大脑
- [x] **input-router.md**（新增）：意图路由，精炼认知 vs 公众号选题自动分流
- [x] **kb-manager.md** 重写：去时效性、去问答引擎、加周扫回答指引
- [x] **writer.md** 简化：只选题探讨+方向确认+素材存 writing/

### 本地（公众号写作助手）

- [x] **CLAUDE.md** 重写：纯写作定位，引用 XQ_writing_style_prompt.md 和 wechat-ai-writing-skill.md
- [x] **与服务器 CC 协同**：启动时检查 writing/ 有无待续选题

## 引擎管线（已完成）

- [x] hermes_engine.py：三阶段事务（Move→Process→Commit）
- [x] AI 分类（knowledge_type + usage_tag + 评分）
- [x] URL 并行抓取（ThreadPoolExecutor max_workers=3）
- [x] collect_background_context 语义检索
- [x] 死信队列（3 次重试上限）
- [x] 细胞分裂（mixed 类型双写）
- [x] 关联计算（同 tag +3 / 标题重叠≥3字 +1 / 双向写入）
- [x] 问答回路：Dashboard → 回答问题
- [x] 引擎 DeepSeek → 阿里百炼 DashScope（qwen3.6-flash）
- [x] **去 timeliness 字段**：prompt、JSON 输出、frontmatter 写入全部删除
- [x] **processing 重试修复**：Step 1.5 扫描 processing 已有文件

## 已回答问题处理（已完成）

- [x] answer/save 不再写 inbox，回答只保存到 question 文件
- [x] collect_answered_questions()：扫描已回答问题 → 送周扫审计上下文
- [x] 流转链路：CC 回答 → sync.sh push → GitHub → 本地 pull → 静态站可见 → 周扫感知
- [x] POST /question/discard 路由：删除文件 + 清理 state.json

## 静态站（已完成）

- [x] build_static.py：渲染 + Pagefind 搜索
- [x] 浏览页按 tag 分组（principle/tool/reflection/opinion/case/reference）
- [x] 同 tag 内按 mtime 降序
- [x] 标签中文显示
- [x] **frontmatter 不显示修复**：\_strip_frontmatter() 剥离 YAML 和首行 # 标题
- [x] **相关链 404 修复**：\_replace_md_link() .md→.html
- [x] **标题重复渲染修复**：\_strip_frontmatter() 同时剥离首行 # 标题
- [x] extract_meta 优先读 # 标题（Topic 标题中文）

## 周扫（已完成）

- [x] weekly_scan.py：摘要级审计 + Dashboard 双区管理
- [x] AUDIT_PROMPT 重写：从"学术导师"→"务实的认知审计员"
- [x] 审计目标：实践盲区 / 判断缺口
- [x] 输出上限 3→2 条
- [x] temperature 0.4→0.1
- [x] 去掉 `【认知张力】` 分类前缀
- [x] 已回答问题作为上下文送 AI 审计

## 编译器

- [x] compiler.py：语义+标签聚类 → AI 合成主题页 → core/topic/
- [x] 覆盖制（每周覆盖，不保留历史）
- [x] 往前编不往回织

## 已停用

- [x] ~~动态 Web 服务（Flask + Cloudflare Tunnel → zs.captainxq.me）~~ — 2026-06-15 停用
- [x] ~~hermes-web 服务~~ — 已禁用自启
- [x] ~~hermes-tunnel 服务~~ — 已禁用自启
- [x] ~~问答引擎（/ask、/chat）~~ — 手机 CC 直连服务器替代
- [x] ~~PC Agent 对话助手~~ — 不推进
- [x] ~~移动端体验优化~~ — 不推进

# 技术债

- 密码保护暂缓：当前仅配了 CF Tunnel 访问控制

# 下一阶段

按需扩展：知识图谱可视化、面板统计
